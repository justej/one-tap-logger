<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="datepicker_sleep" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/sleep</item>
        <item name="colorPrimaryDark">@color/sleep</item>
        <item name="colorAccent">@color/sleep</item>
    </style>

    <style name="datepicker_wake_up" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/wake_up</item>
        <item name="colorPrimaryDark">@color/wake_up</item>
        <item name="colorAccent">@color/wake_up</item>
    </style>
</resources>
